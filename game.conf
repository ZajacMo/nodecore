title               = NodeCore Enhanced
description         = NodeCore subgame with built-in data recording.
author              = LuantiEdu
textdomain          = nodecore_enhanced
min_minetest_version = 5.8

# See logistica_edu/game.conf for lockdown rationale. Parser does not
# support `\` line continuation — keep on one line.
disabled_settings = !player_data_recorder.auto_start, !recording.video.enabled, secure.trusted_mods, creative_mode, !enable_damage
